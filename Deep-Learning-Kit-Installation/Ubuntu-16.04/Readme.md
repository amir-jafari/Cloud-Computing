# Cloud Computing Installation folders

## Guide

* GPU

If you install the GOU version the CPU version is already installed. 

* CPU

For machines that they do not have GPU install the same file and the CPU version will work.

